你是一个专业的图表分析助手。我将提供一张图表图片、对应的 OCR 文本检测结果以及一组关键点坐标（Keypoints）。你的任务是结合这些信息，完成以下三个分析步骤，并将结果以严格的 JSON 格式输出。

### 输入信息
1.  **OCR 检测结果**（格式为 List[[text, [cx, cy, w, h]]]）：
    【OCR_RESULT】
2.  **关键点坐标**（格式为 List[[x, y]]，已在图中用红点标出）：
    【KEYPOINTS】

### 分析任务

**任务 1：图表类型分类**
首先判断图表类型。逻辑如下：
1.  如果存在 `Datapoint` 类型的文本（即图表元素上直接标了数值），则归类为 `has_text`。
2.  如果没有 `Datapoint` 类型的文本，需根据图表视觉特征归类为以下之一：
    -   `pie` (饼图/环形图)
    -   `line` (折线图)
    -   `vertical_bar` (垂直柱状图)
    -   `horizontal_bar` (水平条形图)

**任务 2：文本语义分类与补全**
结合图片视觉信息和 OCR 结果，执行以下操作：
1.  **文本分类**：将 OCR 识别到的所有文本归类为：
    -   `Title`: 主标题/副标题
    -   `Legend`: 图例
    -   `Datapoint`: 直接标注在图表元素上的数值
    -   `Axis_Title`: 坐标轴标题 (区分 X/Y)
    -   `Axis_Tick`: 坐标轴刻度标签 (区分 X/Y)
    -   `Other`: 其他无关文本
2.  **检查漏检与补全**：观察坐标轴刻度是否有明显的文本遗漏。如果有，请根据上下文推理补全该文本内容。

**任务 3：坐标轴刻度精细化匹配**
这是最关键的步骤。你需要将识别到的（或补全的）`Axis_Tick` 文本与输入的【KEYPOINTS】进行匹配。
1.  **关键点筛选与分类**：在提供的关键点列表中，分辨出哪些属于 X 轴刻度点，哪些属于 Y 轴刻度点。请剔除不在轴线上或明显偏离主结构的干扰点。
2.  **坐标对齐**：
    -   对于每一个 X 轴刻度文本，找到其对应的 X 轴关键点坐标 `[x, y]`。
    -   对于每一个 Y 轴刻度文本，找到其对应的 Y 轴关键点坐标 `[x, y]`。
    -   **注意**：最终输出的坐标必须优先使用输入的【KEYPOINTS】中的数值，而不是 OCR 文本框的中心点，除非该刻度完全没有对应的关键点。
3.  **遗漏推理**：如果发现某个刻度文本在【KEYPOINTS】中没有对应的点（即关键点漏标），请根据已有关键点的分布规律（如等间距、线性分布），推理出该刻度应有的关键点坐标。
4.  **轴属性分析**：
    -   `type`: 判断轴是 "numerical" (数值轴) 还是 "categorical" (类别轴)。
    -   `unit`: 提取坐标轴的单位（如 %, 元, km 等，若无则为空字符串）。

### 输出格式
请严格遵守以下 JSON 结构。

**注意：**
1.  `x_axis` 和 `y_axis` 的 `ticks` 必须包含文本和**对应的关键点坐标 `[x, y]`**。
2.  其他字段（title, legend, datapoint）仅输出文本列表。

<answer>
{
    "chart_type": "has_text/pie/line/vertical_bar/horizontal_bar",
    "x_axis": {
        "type": "numerical/categorical",
        "title": ["标题文本", ...],
        "ticks": [
            ["刻度文本1", [x1, y1]],
            ["刻度文本2", [x2, y2]],
            ...
        ]
    },
    "y_axis": {
        "type": "numerical/categorical",
        "title": ["标题文本", ...],
        "ticks": [
            ["刻度文本1", [x1, y1]],
            ["刻度文本2", [x2, y2]],
            ...
        ]
    },
    "other_text": {
        "title": ["主标题文本", ...],
        "legend": ["图例文本", ...],
        "datapoint": ["数据点文本1", "数据点文本2", ...],
    }
}
</answer>