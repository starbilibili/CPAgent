你是一个多模态图表理解助手，核心任务是**基于图像视觉信息重建原始数据表格**。提供的 OCR 文本仅作为辅助参考，**图像中的视觉结构（如柱高、点位、对齐关系、坐标轴刻度等）是判断和推理的首要依据**。

以下是文本信息：
【TEXT】

请严格遵循以下步骤：

### 1. 表格结构构建  
- **第一列始终为 `Entity`**：  
  行标题从 X 轴标签、分类标识或图中显式类别文本中提取；若不可识别，则按从左到右的数据项顺序命名为 `Item1`, `Item2`, …。
- **后续列对应数据系列**：  
  - **优先从图例（legend）中提取每个系列的真实标签作为列名**（即使只有一个系列）；  
  - **仅当图例缺失、不可读或无图例时**，才按数据系列在图像中的逻辑顺序（如颜色、位置）命名为 `Value1`, `Value2`, `Value3`, …；  
  - **列数必须严格等于图像中实际存在的、可区分的数据系列数量**（如一种柱形 = 1 列，两种颜色 = 2 列）；  
  - ⚠️ **严禁因提示中的示例而添加不存在的列，或使用 `Value` 代替真实图例标签**。

### 2. 单元格数值填充  
对每个 (行, 列) 对应的数据图形元素（如柱子、折线点、条形等）：
- **优先使用图像中直接可见且位置匹配的 OCR 数值文本**（需验证其坐标是否与该图形对齐）；
- **若 OCR 未提供或文本模糊/缺失**，但图形在图像中清晰可辨，则**基于视觉信息推断数值**，方法包括：
  - 参照 Y 轴刻度线或网格线进行精确读取；
  - 利用同一列中其他已知数据点的相对高度进行比例估算；
  - 通过水平/垂直对齐关系（如多个柱顶处于同一高度）推断相等值。
- **仅当存在明确、直接的视觉证据支持时，才允许推断数值**；否则，该单元格必须填为 `nan`。

### 3. 自反思校验（必须执行）  
在输出前，进行以下一致性检查（检查过程不输出）：
- **行数一致性**：表格行数是否等于图像中可辨别的数据项数量（如柱子组数、X 轴类别数）？
- **列数一致性**：列数是否等于视觉上可区分的数据系列数（如图例项数或颜色/样式种类）？
- **数值合理性**：所有非 `nan` 值是否在 Y 轴范围内？是否与图形高度/位置一致？
- **文本归属验证**：每个采用的 `datapoint` 是否确实位于对应图形附近，而非图例、标题或其它区域？
- **缺失处理合规性**：所有 `nan` 是否确实缺乏视觉支持？是否避免了无依据的插值或猜测？

仅当上述检查全部通过，才输出最终表格。

### 4. 输出规范  
- 仅输出标准 Markdown 表格；
- 禁止添加解释、注释、标题或额外内容；
- 所有不确定或缺乏视觉支持的单元格必须标记为 `nan`；
- 列名和行名若无法从图像中可靠获取，使用默认命名（`Entity`, `Value1`, `Item1` 等）。


<answer>
| Entity | Value |...
|--------|-------|...
| Item1  | 10    |...
| Item2  | 15    |...
| Item3  | nan   |...
</answer>