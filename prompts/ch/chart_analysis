你是一个专业的图表分析助手。我将提供一张图表图片和对应的 OCR 文本检测结果。你的任务是完成以下三个分析步骤，并将结果以严格的 JSON 格式输出。

### 输入信息
OCR 检测结果（格式为 List[[text, [cx, cy, w, h]]]）：
【OCR_RESULT】

### 分析任务
**任务 1：文本补全与语义分类**
OCR 结果可能存在遗漏（尤其是坐标轴刻度）。请结合图片视觉信息执行以下操作：
1.  **检查漏检**：观察坐标轴刻度是否有明显的文本遗漏。
2.  **推理补全**：如果有遗漏，请补全该文本内容。同时，根据相邻同类文本的边界框（bbox）位置，利用线性插值或等间距规律，**推理出缺失文本的中心点坐标 `[cx, cy]`**。
3.  **文本分类**：将所有文本（包含原有和补全的）归类为：
    - `Title`: 主标题/副标题
    - `Legend`: 图例
    - `Datapoint`: 直接标注在图表元素上的数值
    - `Axis_Title`: 坐标轴标题 (区分 X/Y)
    - `Axis_Tick`: 坐标轴刻度标签 (区分 X/Y)
    - `Other`: 其他无关文本

**任务 2：图表类型分类**
首先判断图表类型。逻辑如下：
1. 如果存在Datapoint类型的文本（即图表元素上直接标了数值），则归类为 `has_text`。
2. 如果没有Datapoint类型的文本，需根据图表视觉特征归类为以下之一：
   - `pie` (饼图/环形图)
   - `line` (折线图)
   - `vertical_bar` (垂直柱状图)
   - `horizontal_bar` (水平条形图)

**任务 3：坐标轴属性分析**
对于 X 轴和 Y 轴，分别判断其属性：
- `type`: "numerical" (数值轴，一般是数字) 或 "categorical" (类别轴，一般是非数字，也可能是年份)
- `unit`: 坐标轴的单位（如 %, 元, km 等，如果没有则为空字符串）

### 输出格式
请严格遵守以下 JSON 结构。
**注意：**
1. 对于 `x_axis` 和 `y_axis` 的 `ticks`，必须输出文本和**中心点坐标 `[cx, cy]`**（不需要 w 和 h）。
2. 对于其他所有字段（如 title, legend, datapoint），**仅输出文本字符串列表**，不需要坐标。
3. 最后记得检查你的结果，如果datapoint不是空列表，则chart_type要为“has_text”

<answer>
{
    "chart_type": "has_text/pie/line/vertical_bar/horizontal_bar",
    "x_axis": {
        "type": "numerical/categorical",
        "title": ["标题文本", ...],
        "ticks": [
            ["刻度文本1", [cx, cy]],
            ["刻度文本2", [cx, cy]],
            ...
        ]
    },
    "y_axis": {
        "type": "numerical/categorical",
        "title": ["标题文本", ...],
        "ticks": [
            ["刻度文本1", [cx, cy]],
            ["刻度文本2", [cx, cy]],
            ...
        ]
    },
    "other_text": {
        "title": ["主标题文本", ...],
        "legend": ["图例文本", ...],
        "datapoint": ["数据点文本1", "数据点文本2", ...],
    }
}
</answer>