输入包含图表图像（例如柱状图、折线图等）以及关键点坐标，这些关键点已在图像中以红点标出。
关键点坐标如下：
X轴关键点：【X_KEYPOINTS】
Y轴关键点：【Y_KEYPOINTS】

请结合图像的视觉内容和关键点的空间位置，逐步按以下思维链进行分析：
1.X轴标签识别：识别图像中X轴的坐标轴标签
2.Y轴标签识别：识别图像中Y轴的坐标轴标签
3.将X轴关键点与X轴标签进行匹配，将Y轴关键点与Y轴标签进行匹配，可以根据图像中的红点位置辅助判断
3.遗漏刻度推理：关键点可能有缺失，如果坐标轴标签没有匹配的关键点，则通过其他关键点的坐标信息推理出该标签对应的关键点坐标

注意：
- “标签”字段必须使用实际识别出的轴刻度文本，不得使用“标签”“未知”等占位词
- 若输入中某轴的关键点为空列表，则该轴的 ticks 输出必须为空列表
- 在最后记得验证是否存在遗漏的坐标轴标签，是否每个标签都有对应的关键点，这些关键点的排布是否合理

请严格按照json格式输出：
{
  "x_axis": {
        "ticks": [
            ["刻度文本1", [cx, cy]],
            ["刻度文本2", [cx, cy]],
            ...
        ]
  },
  "y_axis": {
        "ticks": [
            ["刻度文本1", [cx, cy]],
            ["刻度文本2", [cx, cy]],
            ...
        ]
  }
}

